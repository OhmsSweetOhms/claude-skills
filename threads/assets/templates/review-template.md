# Thread status review — {{DATE}}

**Snapshot date:** {{DATE}}
**Source of truth:** [`threads.json`](threads.json) — this review is a frozen snapshot; thread states evolve faster than this document.
**Tool:** `~/.claude/skills/threads/scripts/status_review.py`
**Predecessor review:** {{PREV_REVIEW_LINK_OR_NONE}}

<!--
This file has two halves:
- Sections 1-4 are AUTO-GENERATED by status_review.py from threads.json + per-thread manifests. Regenerated each review pass; do not hand-edit them — your edits will be lost on the next regen.
- Section 5+ is MANUAL. Strategic-tier grouping, parallelism notes, dependency graph, critical-path observations. Inherits forward across review files; copy/edit when running a new review.

The auto/manual split is enforced by the AUTO-BEGIN/AUTO-END markers below. status_review.py rewrites only the content between those markers; everything before/after is preserved.
-->

<!-- AUTO-BEGIN -->

## 1. Status counts

{{STATUS_COUNTS_TABLE}}

## 2. By subsystem

{{BY_SUBSYSTEM_TABLE}}

## 3. Active threads ({{ACTIVE_COUNT}})

Sorted by `updated` date, most recent first.

{{ACTIVE_THREADS_TABLE}}

## 4. Triage candidates ({{TRIAGE_COUNT}})

Threads whose state suggests they need attention. **Auto-flagged; disposition is a Claude+user decision.** For each candidate, propose `keep_active` / `close_pass` / `close_fail` / `close_inconclusive` / `supersede` / `unblock`, then route accepted closures through the **Close-thread** workflow.

{{TRIAGE_TABLE}}

<!-- AUTO-END -->

<!-- MANUAL — sections below this line are hand-curated. Auto-regen preserves them. -->

## 5. Strategic execution tiers

Group active threads by execution priority + dependency. Auto-status above tells you *what state* threads are in; this section tells you *which to work on next* and *what gates what*.

| Tier | Name | Threads | Parallel? | Gates on |
|------|------|---------|-----------|----------|
| **1** | *(Highest-payoff critical path — usually 1-3 threads)* | *(thread ids)* | *(N-way parallel / sequential)* | *(prerequisites or "nothing — start anytime")* |
| **2** | *(Next-highest priority — often spec-only / parallel-safe)* | | | |
| **3** | *(Sequential execution chain — wall-clock bottleneck)* | | | |
| **N** | *(Ancillary / orthogonal — runs alongside everything)* | | | |

> Delete unused rows. Add tiers as the project grows. A "tier" is a strategic grouping, not a status — threads can move between tiers without changing their `active`/`blocked`/`closed` status.

## 6. Cross-tier file-overlap concerns

Threads in different tiers that touch overlapping files. List only when serialization is needed; most thread pairs are file-disjoint and parallelizable.

| Hop / thread | File domain | Conflicts with | Resolution |
|--------------|-------------|----------------|------------|
| *(thread / plan-hop)* | *(file or directory)* | *(other thread)* | *(serialize / coordinate / fold-into)* |

## 7. Critical-path observations

> Free-form prose. What's the bottleneck? What's the highest-payoff-per-effort? Where does wall-clock time accumulate? What unblocks downstream work?

## 8. Concrete recommendation by goal

| Goal | Start here |
|------|------------|
| **Biggest visible win, fastest** | *(Tier-N → thread)* |
| **Maximize parallel-session productivity** | *(Tier-N → which threads to run concurrently)* |
| **Clear backlog** | *(Tier-N orthogonal threads)* |

## 9. Notes / progress journal

> Reverse-chronological prose. Optional. What changed since the prior review file? What's the take-home for the next reviewer?
